# Description
Unit Test I/O Operation Utility Service
    
# Features
## 1.0.0
### Added tests
 + try to let unit tests be compatible with .NET Standard 2.0 and other .NET versions
          (but not tested yet, so may be not compatible with .NET Standard 2.0)